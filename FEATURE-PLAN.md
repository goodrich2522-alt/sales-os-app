# FEATURE-PLAN.md — แผนพัฒนาแอปรอบใหม่ (UI + 3 ฟีเจอร์)

> **เขียนเมื่อ:** 27 ก.ค. 2569 · ยังไม่ลงมือแก้โค้ด — รอผู้ใช้อนุมัติแผนก่อน
> **โยงกับ:** [ROADMAP.md](ROADMAP.md) (แผนใหญ่ A-G) · งานนี้ลงรายละเอียดของ 4 เรื่องที่ผู้ใช้ขอ
> **กติกา:** ทำทีละเฟส · จบเฟสให้ทดสอบบนเว็บจริง (โดยเฉพาะมือถือ — ทีมใช้มือถือเป็นหลัก) · อัปเดต ☐ → ☑

---

## 🔎 สิ่งที่พบจากการตรวจแอปทั้งหมด

| หน้า | บรรทัด | ปัญหา UI ที่เจอ |
|------|--------|-----------------|
| `sales/main` | **1,697** | ไฟล์เดียวยาวมาก · ตัวกรอง/ค้นหา/ป๊อปอัพ/ฟอร์มปนกันหมด · แก้ทีละจุดเสี่ยงพัง |
| `stock/main` | **1,266** | ค้นหามีแค่ช่องเดียว + กรองหมวด/สถานะ · ยังไม่มี sort ไม่มีนับสต็อกเหลือ |
| `dashboard` | 830 | มีกราฟ 7 ตัวแล้ว (recharts) แต่ดูรายเดือนอย่างเดียว · ไม่มีรายวัน/สัปดาห์/ปี · แยกแบรนด์/รุ่นไม่ได้ |
| `transporter/main` | 705 | ใช้ผ่าน RPC แล้ว · UI พอใช้ได้ |

**รากของปัญหา UI:** หน้าใหญ่ 2 หน้าไม่เคยถูกแยก component เลย ทำให้ทั้งแก้ยากและ UI ไม่สม่ำเสมอ
→ **ต้องแยก component ก่อน แล้วค่อยปรับหน้าตา** ไม่งั้นยิ่งแก้ยิ่งพัง (ตรงกับ DEV-PLAN เฟส 3)

**ข้อจำกัดที่ต้องออกแบบรอบ:**
- **Static export ไม่มี server** — อ่าน PDF/รูปต้องทำฝั่ง browser (pdf.js) หรือผ่าน GAS ที่มีอยู่
- **RLS เปิดแล้ว** — ทุกหน้าต้องล็อกอิน (ผู้ขนส่งใช้ RPC)
- **มือถือเป็นหลัก** — ทุกอย่างต้องแตะง่าย นิ้วโป้งเอื้อมถึง

---

## 🎨 เฟส 1 — วางระบบ UI ให้เป็นชุดเดียวกัน (ราก ต้องทำก่อน)

> เป้าหมาย: สร้าง "ภาษากลาง" ของ UI เพื่อให้ทุกหน้าหน้าตาเหมือนกันและแก้ครั้งเดียวมีผลทุกหน้า
> ไม่ใช่การรื้อ — เป็นการดึงของซ้ำออกมาเป็นชิ้นส่วนกลาง

- ☐ 1.1 สร้าง `components/ui/` — ชิ้นส่วนพื้นฐานที่ใช้ซ้ำทั้งแอป: `<Button>`, `<Input>`, `<Select>`, `<Modal>`, `<Card>`, `<Badge>` (ป้ายสถานะสีเดียวกันทุกหน้า), `<Chip>` (แท็กกดได้), `<EmptyState>`, `<Toast>`
- ☐ 1.2 สร้าง `lib/constants.ts` — รวมสีสถานะ (`STATUS_BADGE`), สีแหล่งที่มา, ชื่อเดือนไทย ที่ตอนนี้ซ้ำ 3-4 ที่ (DEV-PLAN เฟส 3)
- ☐ 1.3 สร้าง `lib/format.ts` — รวม `thaiDate`, `formatBaht`, `daysUntil`, `specCode` ที่ join ซ้ำหลายที่
- ☐ 1.4 แยก `<Lightbox>` (ดูรูปเต็มจอ) ที่ซ้ำอยู่ 4 ชุด (sales/transporter/dashboard) เป็นตัวเดียว
- ☐ 1.5 กำหนด design token: สี/ระยะห่าง/ตัวอักษร/มุมโค้ง เป็นค่ามาตรฐาน (ต่อยอดจาก Tailwind config)
- **ผลลัพธ์:** ชุดชิ้นส่วน UI พร้อมใช้ · หน้าเดิมยังทำงานเหมือนเดิม (ยังไม่เปลี่ยนหน้าตาที่ผู้ใช้เห็น)
- **ประเมิน:** กลาง-ใหญ่ · ตัดโค้ดซ้ำได้ ~600-800 บรรทัดตามที่ DEV-PLAN ประเมินไว้

---

## 🔍 เฟส 2 — หน้าค้นหาสต็อกแบบใหม่ (ข้อ 4 ของผู้ใช้)

> เป้าหมาย: หาสินค้าในสต็อกได้เร็ว รู้ทันทีว่ารุ่นไหนเหลือ/หมด · ใช้ชิ้นส่วน UI จากเฟส 1

- ☐ 2.1 **แถบค้นหาอัจฉริยะ** — พิมพ์คำเดียวค้นได้ทั้ง SN / รุ่น / ยี่ห้อ / PI / ลูกค้า (debounce ไม่หน่วง)
- ☐ 2.2 **แท็กกรองแบบกด(facet chips)** — กดเลือกได้หลายเงื่อนไขพร้อมกัน:
  - ชนิดสินค้า (Forklift / Stacker / Handlift)
  - ยี่ห้อ (HELI / HANGCHA / STAXX / เจนบรรเจิด)
  - สถานะ (พร้อมขาย / จอง / ขายแล้ว / รถเช่า / สั่งผลิต ...)
  - แต่ละแท็กโชว์ตัวเลขกำกับ เช่น "พร้อมขาย (200)"
- ☐ 2.3 **มุมมองรวมตามรุ่น** — จัดกลุ่มรถตามรุ่น โชว์ "เหลือกี่คัน / ขายไปกี่คัน" ต่อรุ่น
  - **ป้ายเตือนของใกล้หมด** (เหลือ ≤ 2 คัน = แดง · หมด = เทา · เหลือเยอะ = เขียว)
  - ตรงกับที่ผู้ใช้ขอ "ดูว่าคันไหนไม่เหลือ คันไหนเหลือเยอะ"
- ☐ 2.4 **เรียงลำดับ (sort)** — ตามวันรับล่าสุด / ราคาทุน / จำนวนคงเหลือ / ตัวอักษร
- ☐ 2.5 **สลับมุมมอง** การ์ด ↔ ตาราง (ตารางเหมาะกับดูเยอะๆ บนจอใหญ่)
- ☐ 2.6 จำสถานะตัวกรองไว้ (กลับมาหน้าเดิมแล้วยังกรองอยู่)
- **ผลลัพธ์:** หน้าสต็อกที่หาของเจอใน 2-3 วินาที · เห็นภาพรวมคงเหลือทันที
- **ประเมิน:** กลาง · ข้อมูลพร้อมหมดแล้ว (642 คันใน DB)

---

## 📊 เฟส 3 — แดชบอร์ดยอดขาย (ข้อ 3 ของผู้ใช้)

> เป้าหมาย: ดูยอดขายทุกช่วงเวลา + รู้ว่ารุ่นไหนขายดีเพื่อวางแผนสต็อก
> **ข้อมูลพร้อมแล้ว:** ตาราง `sales` มี `actual_sale` + `delivery_date` + `forklift_brand/model` ครบ (ยอดจริง 45.5 ล้าน)

- ☐ 3.1 **ตัวเลือกช่วงเวลา** — ปุ่มสลับ รายวัน / สัปดาห์ / เดือน / ปี (กราฟเส้นหรือแท่ง อ่านง่าย)
  - มีกราฟรายเดือน/สัปดาห์อยู่แล้วใน `Charts.tsx` — ต่อยอดเพิ่มรายวัน/รายปี
  - ⚠️ แก้บั๊กเดิม: กราฟรายเดือนไม่แยกปี (DEV-PLAN 4.4) — ยอดคนละปีบวกรวมกัน
- ☐ 3.2 **การ์ดสรุปด้านบน** — ยอดขายเดือนนี้ · จำนวนคันที่ขาย · กำไรขั้นต้น · เทียบเดือนก่อน (↑↓ กี่ %)
- ☐ 3.3 **ยอดขายแยกยี่ห้อ** — กราฟวงกลม/แท่ง ดูสัดส่วน HELI vs HANGCHA vs STAXX ...
- ☐ 3.4 **อันดับรุ่นขายดี (สำคัญที่สุดสำหรับวางแผนสต็อก)** — ตารางเรียงรุ่นที่ขายดีสุด พร้อม:
  - จำนวนคันที่ขาย · ยอดขายรวม · กำไรเฉลี่ยต่อคัน · เหลือในสต็อกกี่คัน
  - **สัญญาณเตือนสต็อก:** รุ่นขายดีแต่สต็อกเหลือน้อย = ควรสั่งเพิ่ม
- ☐ 3.5 **กราฟกำไร** — ยอดขาย vs ต้นทุน ต่อเดือน (เห็นกำไรขั้นต้นเป็นเทรนด์)
- ☐ 3.6 **แยกรายเซลล์** — มีอยู่แล้วบางส่วน · เพิ่มยอดขายรายคน/รายเดือน
- ☐ 3.7 **ปุ่ม export** — ดาวน์โหลดรายงานเป็น CSV/รูป (ผู้บริหารเอาไปใช้ต่อ)
- **ผลลัพธ์:** เปิดหน้าเดียวรู้สุขภาพธุรกิจ + รู้ว่าควรสต็อกรุ่นไหนเพิ่ม
- **ประเมิน:** กลาง-ใหญ่ · กราฟมีของเดิมให้ต่อยอด

---

## 📄 เฟส 4 — อ่านใบเสนอราคาผู้ผลิต → กรอกอัตโนมัติ (ข้อ 2 ของผู้ใช้)

> เป้าหมาย: อัปโหลด PDF/รูปใบเสนอราคา → ระบบอ่านรายชื่อรถ → กรอกลงช่องให้ → คนตรวจแล้วกดยืนยัน

### ✅ ผลวิเคราะห์ตัวอย่างจริง 5 ไฟล์ / 4 เจ้า (27 ก.ค. 2569)

| ไฟล์ | ผู้ผลิต/แบรนด์ | ชนิด | โครงสร้าง |
|------|--------------|------|-----------|
| `C20726201-001` | **HELI** (Comex) | text layer 2,791 ตัวอักษร | Sales Contract 1 คัน/ใบ · มี SN จริง (`010353N6726`), รุ่น, ทุน, มาสต์, วาล์ว |
| `NBPI20261010` | **STAXX** (Ningbo) | text layer 8 หน้า | Proforma ล็อตใหญ่ **13 รุ่น** · Model/จำนวน/ราคา FOB/สเปกเต็ม |
| `QO-2025090114` | **ROCKMAN** (CNC-moving) | text layer แต่ **ฟอนต์ไทยเพี้ยน** | ตัวเลข/รุ่น/ราคาอ่านได้ · ภาษาไทยเป็นหัวกระดาษ/เงื่อนไข ไม่กระทบ |
| `HCTH-BFL...082` + `.jpg` | **HANGCHA** | 🔴 สแกน (0 ตัวอักษร) | **แต่ vision AI อ่านออกครบทุกช่อง** — ดูด้านล่าง |

### 🔒 หลักการที่ผู้ใช้กำหนด (27 ก.ค.): ไม่พึ่ง LLM ภายนอก · ข้อมูลไม่ออกนอกองค์กร · ใช้ **Typhoon OCR 1.5 (SCB 10X) บนเซิร์ฟเวอร์เราเอง**

**กุญแจสำคัญคือ 3 ใน 4 เจ้าไม่ต้อง OCR เลย** — PDF มี text layer อยู่แล้ว อ่านตรงด้วย pdf.js ในเบราว์เซอร์ (ฟรี แม่น 100%)
เหลือ **HANGCHA เจ้าเดียวที่เป็นสแกน** → ส่งไปให้ Typhoon OCR บนเซิร์ฟเวอร์ของบริษัทอ่าน

**ทำไม Typhoon OCR 1.5 (ดีกว่า EasyOCR ที่คุยกันก่อนหน้า):**
- เป็น **vision-language model (VLM)** ไม่ใช่ OCR แบบเก่า → **เข้าใจโครงสร้างตาราง คืนผลเป็น Markdown/มีโครงสร้างได้เลย** (EasyOCR คืนแค่ข้อความ+ตำแหน่ง ต้องเขียน parser แยก field เยอะ)
- **optimized สำหรับภาษาไทยโดยเฉพาะ** (SCB 10X ทำเพื่อเอกสารไทย) → HANGCHA ที่ปนไทย/อังกฤษ/จีน + ตาราง อ่านแม่นกว่ามาก
- **open + self-host ได้** ผ่าน Ollama หรือ vLLM (OpenAI-compatible endpoint) → ข้อมูลไม่ออกนอกองค์กร
- โมเดลเล็ก **2B พารามิเตอร์** → รันบนเซิร์ฟเวอร์เล็กได้ (GPU เล็กหรือ CPU)

### สถาปัตยกรรม — client-side อ่าน text layer + Typhoon OCR บนเซิร์ฟเวอร์เราเอง

```
เครื่องทีม (คอมของ user เอง — ไม่มีเซิร์ฟเวอร์แยก)
────────────────────────────────────────────────
เบราว์เซอร์ (แอป static บน GitHub Pages)
ไฟล์เข้า → [เดาผู้ผลิต] ─┬─ text layer (HELI/STAXX/ROCKMAN) → pdf.js อ่านในเบราว์เซอร์ 100%
                        │                                      (ไม่ต้องพึ่งอะไรเลย)
                        └─ สแกน (HANGCHA) → pdf.js render เป็นรูป
                                          → Ollama บนเครื่อง user (http://localhost:11434)
                                            typhoon-ocr1.5 → คืน Markdown
        → [parser แยกเจ้า] แปลงเป็นรายการรถ
        → [หน้าคนตรวจ + ยืนยัน]
        → บันทึกเข้า Supabase ตาม SN-RULES.md
```

### สถาปัตยกรรมจริง (ปรับ 27 ก.ค.): มีแค่ **เครื่องทีม + Supabase** ไม่มีเซิร์ฟเวอร์แยก

- **3/4 เจ้า (HELI/STAXX/ROCKMAN) = text layer** → pdf.js อ่านในเบราว์เซอร์ล้วน ทำได้ทันทีไม่ต้องติดตั้งอะไร ✅
- **HANGCHA (สแกน) = ทางเลือก** เมื่อจำเป็น:
  - (ก) ติดตั้ง **Ollama บนคอมเครื่องทีม** (Windows) + โมเดล typhoon-ocr · แอปเรียก `http://localhost:11434` ตรงๆ (localhost = secure context เบราว์เซอร์ไม่บล็อก) · ตั้ง `OLLAMA_ORIGINS` ให้โดเมนแอป
  - (ข) หรือ **กรอกมือ** (HANGCHA มาไม่บ่อย) — เร็วกว่าถ้าไม่อยากเปิด Ollama
- ไม่ต้องมี proxy/เซิร์ฟเวอร์ Linux — `ocr-server/` ปรับเป็นคู่มือรัน Ollama บนเครื่อง user (ดู README)

**⚠️ ข้อควรรู้:**
1. HANGCHA ต้องเปิด Ollama บนเครื่องตอนใช้ (ไม่เปิด = กรอกมือ) · ข้อมูลไม่ออกนอกเครื่องเลย
2. งานหลักฝั่งแอปคือ **parser text layer ต่อเจ้า** (HELI ก่อน) + หน้าตรวจทาน

### รายการงาน

- ☑ 4.1 (27 ก.ค.) ดูตัวอย่างจริง 4 เจ้า + จำแนกชนิดไฟล์ (3 text layer · HANGCHA สแกน)
- ☑ 4.2 **ตั้งเซิร์ฟเวอร์ Typhoon OCR** (27 ก.ค.) — สร้างชุด deploy ที่ [`ocr-server/`](ocr-server/) : docker-compose (Ollama + proxy) · FastAPI proxy (`/ocr` รับ PDF/รูป → คืน Markdown · auth โทเคน · จำกัด origin · PDF→รูปด้วย PyMuPDF) · README คู่มือ setup ครบ · **รอทีม deploy บนเซิร์ฟเวอร์บริษัท**
- ☑ 4.3 **ฝัง pdf.js ในแอป** (27 ก.ค.) — `lib/quoteImport/pdfText.ts` อ่าน text layer ในเบราว์เซอร์ (lazy-load pdfjs-dist · worker ผ่าน `new URL(..., import.meta.url)` build ผ่าน static export) · `looksScanned()` เดาไฟล์สแกน
- ☑ 4.4 **หน้าอัปโหลด** (27 ก.ค.) — `components/QuoteImport.tsx` : ลากไฟล์ PDF หลายไฟล์ · เดาผู้ผลิต (`detectVendor`) · ตารางตรวจ/แก้รายคัน · ติดธงคันที่ไม่มั่นใจ + SN ซ้ำ · บันทึกเข้าสต็อกผ่าน `addForkliftsBulk` (ปุ่มในหน้า stock "ใบเสนอราคา")
- ☐ 4.5 **ท่ออ่าน** — ✅ text layer เสร็จ (pdf.js ในเครื่อง) · ☐ สแกน (HANGCHA) → Ollama local ยังไม่ต่อ
- ☐ 4.6 **normalize ต่อเจ้า** `lib/quoteParser/` (เบากว่าเดิมเพราะ VLM คืนโครงสร้างแล้ว):
  - ☑ `heli.ts` (27 ก.ค.) — Sales Contract text layer · จับ รุ่น/SN/พิกัด(จากเลขรุ่น)/พลังงาน/MAST/Valve/ราคาทุน/PI · ทดสอบกับใบจริง CPCD35-Q22K2 ถูก 100% · รองรับหลาย SN ต่อใบ · id=SN (SN-RULES)
  - ☑ `staxx.ts` (27 ก.ค.) — (ก) **Serial No. List (Excel)**: จับคอลัมน์เอง · แตกช่วง SN "51596-1~51596-12"→รายคัน · พิกัดจากรุ่น (เลข×100 kg) · ทดสอบ 290 คันถูก · (ข) **Proforma PDF**: `parseStaxxProforma` ดึงราคา FOB USD (ทดสอบ 32 รายการถูก) + `normalizeStaxxModel` merge ราคาเข้ารถ Excel SN ตามรุ่น (เก็บ custom_fields "ราคา FOB (USD)" — ไม่ใช่ราคาทุนบาท)
  - ☑ `rockman.ts` (27 ก.ค.) — QO (cnc-moving) ฟอนต์ไทยแตก → ดึงเฉพาะอังกฤษ/ตัวเลข: รุ่น(PD-400-1500)/พิกัด(400kg)/ความสูง(1500mm)/เลขเอกสาร · ราคากรอกเอง · ไม่มี SN
  - `hangcha.ts` — รับ Markdown จาก Typhoon → จับ รุ่น/ราคา/PI/สเปก
  - `detect.ts` เดาเจ้า + `common.ts` ตัวช่วยร่วม
- ☐ 4.7 **หน้าตรวจทาน + ยืนยัน** (สำคัญสุด) — ตารางที่อ่านได้ **เทียบกับไฟล์ต้นฉบับข้างๆ** · แก้ทีละช่อง · ติดธงช่องที่ไม่มั่นใจ · กด "ยืนยัน" บันทึกทั้งล็อต
- ☐ 4.8 **บันทึกเข้าสต็อก** ตาม [SN-RULES.md](SN-RULES.md):
  - HELI มี SN จริง → id = SN
  - STAXX/ROCKMAN/HANGCHA ยังไม่มี SN รายคัน → รถสั่งผลิต `<PI>#<ลำดับ>` สถานะ "สั่งผลิต" · เก็บ PI/ทุน/สเปก · เติม SN จริงตอนรถมาถึง
- ☐ 4.9 **เก็บไฟล์ต้นฉบับ** — แนบ PDF/รูปไว้กับล็อต ผ่าน GAS→Drive
- **ผลลัพธ์:** นำเข้ารถล็อตใหญ่จากใบเสนอราคาทุกเจ้า — ไม่พึ่ง LLM ภายนอก ไม่ส่งข้อมูลออกนอกองค์กร
- **ประเมิน:** ใหญ่สุด · เพิ่มงาน DevOps (เซิร์ฟเวอร์ Typhoon) · **งานฝั่งแอปเบากว่าแผน EasyOCR** เพราะ VLM คืนโครงสร้างให้แล้ว
- **แหล่ง:** [scb-10x/typhoon-ocr (GitHub)](https://github.com/scb-10x/typhoon-ocr) · [typhoon-ocr1.5-2b (HuggingFace)](https://huggingface.co/scb10x/typhoon-ocr1.5-2b)

---

## 🗺️ ลำดับที่แนะนำ

```
เฟส 1 (ราก UI) ──┬── เฟส 2 (ค้นหาสต็อก)      ← เห็นผลเร็ว ใช้ได้ทันที
                 ├── เฟส 3 (แดชบอร์ด)         ← ข้อมูลพร้อม ทำคู่ขนานได้
                 └── เฟส 4 (อ่านใบเสนอราคา)   ← ใหญ่สุด รอตัวอย่างเอกสาร
```

**เหตุผล:**
1. **เฟส 1 ก่อนเสมอ** — ทั้ง 3 เฟสที่เหลือใช้ชิ้นส่วน UI จากเฟส 1 · ถ้าไม่ทำก่อนจะเขียนซ้ำอีก
2. **เฟส 2 (ค้นหา) เห็นผลเร็วสุด** — ทีมได้ใช้ทันที ข้อมูลพร้อมหมด งานไม่ใหญ่
3. **เฟส 3 (แดชบอร์ด)** ทำคู่ขนานกับ 2 ได้ เพราะแตะคนละหน้า
4. **เฟส 4 ท้ายสุด** — ใหญ่สุด + ต้องรอดูตัวอย่างเอกสารจริงก่อน

## ❓ เรื่องที่อยากให้เคาะก่อนเริ่ม

1. **ทิศทาง UI** — อยากได้แนวไหน? (โมเดิร์นสะอาด / สีสันสดใสแตะง่าย / เน้นข้อมูลแน่นๆ) · ผมทำ mockup ให้ดูก่อนได้
2. **เฟส 4 — ✅ เคาะแล้ว: Typhoon OCR 1.5 (SCB 10X) บนเซิร์ฟเวอร์บริษัท** (VLM ไทย · ไม่พึ่ง LLM ภายนอก · ไม่ส่งข้อมูลออกนอกองค์กร) · ต้องเตรียมเซิร์ฟเวอร์ 1 ตัว (Ollama/vLLM — รายละเอียดในเฟส 4)
3. **ลำดับความสำคัญ** — อยากได้อันไหนใช้ก่อน? ผมแนะนำ ค้นหาสต็อก (เฟส 2) เพราะเห็นผลเร็วสุด · หรือถ้าเร่งเรื่องนำเข้ารถล็อตใหม่ ก็ลุยเฟส 4 ได้
