# FIFO-PLAN.md — ระบบขายรถแบบเข้าก่อน-ออกก่อน (First In, First Out)

> เป้าหมาย: รถที่ **รับเข้าคลังก่อน** ควรถูก **ขายออกก่อน** เพื่อลดรถค้างสต็อกนาน (ต้นทุนจม/เสื่อมสภาพ)
> เกณฑ์ FIFO อิงจาก **`received_date` (วันรับรถเข้า)** เป็นหลัก · ถ้าไม่มีให้ fallback เป็น `created_at` (วันเติมเข้าระบบ)
> ดู [[salesos-app-project]] · เกี่ยวข้องกับ byModel (หน้า sales/stock) และ Checkout Modal

---

## หลักการจับคู่ FIFO
- จัดกลุ่มรถ **พร้อมขาย** ตาม `ยี่ห้อ | รุ่น | เสา(MAST)` (คีย์เดียวกับ byModel)
- ในแต่ละกลุ่ม เรียงตาม `received_date` เก่า→ใหม่ → **คันที่เก่าสุด = "ควรขายก่อน"**
- รถไม่มี `received_date` → ถือว่าใหม่สุด (ต่อท้าย) + เตือนให้ฝ่ายสต็อกเติมวันรับรถ

---

## แบ่งเฟสการทำงาน

### ✅ เฟส 0 — ฐานข้อมูลพร้อม (เสร็จแล้ว)
- มีช่อง `received_date` ในฟอร์มนำเข้าใบเสนอราคา + ตารางสต็อก
- STAXX ล็อตล่าสุดลงวันรับรถแล้ว
- **ค้าง:** รถเก่าจำนวนมากยังไม่มี `received_date` → ต้องเติมย้อนหลัง (ดูเฟส 4)

### ✅ เฟส 1 เสร็จ — ป้าย "ควรขายก่อน (FIFO)" ในหน้าขาย
- การ์ดหน้าขายติดป้าย 🟢 **"ควรขายก่อน (FIFO)"** ที่คันเข้าคลังก่อนสุดต่อกลุ่ม ยี่ห้อ|รุ่น|เสา (พร้อมขาย)
- ป้าย **"ค้างสต็อก X วัน"** ทุกคัน (สีเทา/เหลือง>90/แดง>180) · อิง received_date ไม่มีวันรับ=ใหม่สุด
- `fifoOldestIds` useMemo + `daysInStock()` ใน `app/sales/main/page.tsx`

### ✅ เฟส 2 เสร็จ — เตือนตอนปิดการขาย "ข้ามคิว FIFO"
- โมดัลปิดการขาย: ถ้ารถไม่ใช่คันเข้าคลังก่อนสุดในรุ่นเดียวกัน → แถบเหลืองเตือน
  "ควรขาย SN xxx ก่อน — เข้าคลัง [วันที่] · ค้างสต็อก N วัน" + ปุ่ม "สลับไปคันนั้น →" (openCheckout)
- ไม่บังคับ · ใช้ `fifoOldestByKey`/`fifoKeyOf` ใน `app/sales/main/page.tsx`

### ☐ เฟส 3 — รายงานรถค้างสต็อกนาน (Aging Report)
- หน้า stock/main หรือ dashboard: ตารางรถ **พร้อมขาย** เรียงตามอายุค้างสต็อกมาก→น้อย
- ไฮไลต์รถค้าง > 90 / 180 วัน (สีเหลือง/แดง)
- Export Excel ได้ (ต่อยอดจากปุ่ม Export รายการสินค้า)

### ☐ เฟส 4 — เติม received_date ย้อนหลังให้รถเก่า
- รถเก่าที่ไม่มีวันรับรถ → ใช้ `created_at` เติมให้อัตโนมัติ (ครั้งเดียว) หรือให้ฝ่ายสต็อกทยอยกรอก
- migration SQL: `UPDATE forklifts SET received_date = left(created_at,10) WHERE received_date IS NULL/''`

### ☐ เฟส 5 (ออปชัน) — Dashboard FIFO metrics
- KPI: อายุสต็อกเฉลี่ย, %รถค้าง > 90 วัน, มูลค่าต้นทุนจมในรถค้างนาน
- กราฟแนวโน้มอายุสต็อกรายเดือน

---

## หมายเหตุ
- FIFO เป็น **คำแนะนำ (soft)** ไม่บังคับ — เซลล์บางเคสต้องขายคันที่ลูกค้าเจาะจง SN/สเปก
- คีย์จับกลุ่มต้องรวม **เสา (MAST)** ด้วย (รุ่นเดียวกันเสาต่างกัน = คนละสินค้า)
- ข้อมูลวันรับรถต้องสะอาดก่อน FIFO ถึงจะแม่น (เฟส 4 สำคัญ)
