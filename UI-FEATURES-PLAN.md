# UI-FEATURES-PLAN.md — แผนพัฒนา UI + ฟีเจอร์ใหม่ 4 เรื่อง

> **สร้าง:** 27 ก.ค. 2569 · สถานะ: 🟡 รออนุมัติแผน — **ยังไม่ได้แตะโค้ด**
> ที่มา: ผู้ใช้ขอ 4 เรื่อง — (1) ปรับ UI ให้ใช้ง่ายขึ้น (2) อัปโหลดใบเสนอราคาผู้ผลิต ให้ระบบอ่าน+กรอกให้ คนตรวจแล้วกดยืนยัน (3) แดชบอร์ดยอดขาย วัน/สัปดาห์/เดือน/ปี แยกแบรนด์/รุ่น (4) เมนูค้นหาสต็อกที่ sort/กรอง/แท็กได้
> ลำดับที่เสนอ: **1 → 4 → 3 → 2** (เรียงจากเบาไปหนัก · ข้อ 4 กับ 3 ได้ประโยชน์จาก component กลางที่ทำในข้อ 1 · ข้อ 2 ใหญ่สุดและติดของ 2 อย่าง)

---

## สภาพปัจจุบัน (วัดจริง 27 ก.ค.)

| หน้า | ขนาด | ปัญหาที่รู้แล้ว |
|---|---|---|
| หน้าเซลล์ `sales/main` | **1,697 บรรทัด** | ทุกอย่างอยู่ในไฟล์เดียว — ฟอร์มขาย, ตัวกรอง, Lightbox, Settings modal |
| หน้าสต็อก `stock/main` | **1,266 บรรทัด** | ฟอร์มเพิ่มรถ + เติมล็อต + CSV + ประวัติ ปนกันหมด · ค้นหาได้แค่พิมพ์ + กรองชนิด/สถานะ |
| แดชบอร์ด `dashboard` | 830 บรรทัด | 🐛 กราฟรายเดือนไม่แยกปี (ยอดคนละปีบวกรวมเดือนเดียวกัน — DEV-PLAN 4.4) · ไม่มีมุมมองรายวัน/สัปดาห์ · ไม่แยกแบรนด์/รุ่น |
| หน้าผู้ขนส่ง | 705 บรรทัด | — |
| โค้ดซ้ำข้ามหน้า | ~600-800 บรรทัด | `<Lightbox>` ซ้ำ 4 ชุด · Settings modal ก๊อปกัน ~250 บรรทัด × 2 · helper ซ้ำ (DEV-PLAN เฟส 3) |

ข้อจำกัดโครงสร้าง: **static site ไม่มี server** — ฟีเจอร์อ่าน PDF ด้วย AI ต้องมีตัวกลางเก็บ API key (ดูเฟส 4)

---

## เฟส 1 🎨 ตรวจ + ปรับ UI ทั้งแอป (ข้อ 1)

### 1.1 ตรวจ UI ทุกหน้าจอ → รายงาน `ui-audit.md`
เปิดแอปจริงไล่ทีละหน้า (มือถือ + จอคอม) บันทึกเป็นรายการ: จุดที่กดยาก, ขั้นตอนเกิน, ข้อมูลสำคัญที่จมหาย, ความไม่สม่ำเสมอ (สี/ป้าย/ปุ่มคนละแบบ) · จ