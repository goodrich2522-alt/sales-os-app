# ROADMAP.md — แผนพัฒนา SalesOS-App

> **อัปเดต:** 27 ก.ค. 2569 · เอกสารนี้คือ **ลำดับงานหลัก** ของแอป — รายละเอียดแต่ละเรื่องอยู่ในไฟล์ที่อ้างถึง
> **เอกสารประกอบ:** [DEV-PLAN.md](DEV-PLAN.md) (ช่องโหว่ + บั๊ก + โค้ดซ้ำ) · [DATA-SYNC-PLAN.md](DATA-SYNC-PLAN.md) (ซิงก์ข้อมูลกับ Excel) · [sync-rules.md](sync-rules.md) (กติกาแปลงข้อมูล)
> **กติกา:** ทำทีละเฟส · จบเฟสรายงานผล · อัปเดต ☐ → ☑ + ลงวันที่

---

## 🚦 สถานะปัจจุบัน — ปัญหาที่ต้องรู้ก่อนวางแผน

| ระดับ | ปัญหา | ผลกระทบตอนนี้ |
|-------|-------|---------------|
| 🔴 **P0** | **โค้ดใช้ `unit_no` แต่ DB ใช้คอลัมน์ `SN`** | เพิ่ม/แก้รถผ่านแอปบันทึกไม่ลง DB และไม่ขึ้น error (โค้ดกลืนไว้) · ช่อง SN บนจอว่าง |
| 🔴 **P0** | **policy `open_all` ยังอยู่ครบทั้ง 4 ตาราง** | anon key (ฝังอยู่ใน JS ของเว็บสาธารณะ) **อ่าน/เขียน/ลบข้อมูลได้ทุกตาราง** — ชื่อลูกค้า เบอร์โทร ราคาทุน ยอดขาย เปิดโล่งบนอินเทอร์เน็ต |
| 🟠 **P1** | DB มีรถน้อยกว่า Excel 228 คัน · 51 ดีลชี้ไปที่รถที่ไม่มีจริง | แดชบอร์ด/รายงานสต็อกไม่ตรงความจริง · รถสั่งผลิต 6.6 ล้านมองไม่เห็นในระบบ |
| 🟡 **P2** | บั๊กค้าง 6 จุด + โค้ดซ้ำ ~600-800 บรรทัด | ใช้งานได้แต่มีจุดสะดุด · แก้ของใหม่ช้าลง |
| 🟢 **P3** | ธุรกิจ 3 ขา (ขาย/เช่า/ซ่อม) แต่แอปรองรับแค่ขาย | รายได้เช่า + ซ่อม ยังไม่มีระบบรองรับเลย |

---

## เฟส A 🔴 — ยกเลิก `unit_no` เปลี่ยนเป็น `SN` ทั้งระบบ

> **ตัดสินใจแล้ว (27 ก.ค.):** ใช้ `SN` เป็นชื่อมาตรฐาน ให้ตรงกับคอลัมน์จริงใน DB และตรงกับหัวคอลัมน์ใน Excel
> **ทำก่อนทุกอย่าง** เพราะเป็นทั้งตัวปลดล็อกทีมสต็อก และเงื่อนไขก่อนซิงก์ข้อมูล (เฟส B)

### ⚠️ ระวัง — `unit_no` มี 3 ความหมาย เปลี่ยนแค่ตัวเดียว

| ที่ใช้ | คอลัมน์จริงใน DB | ทำอย่างไร |
|--------|-----------------|-----------|
| `Forklift.unit_no` | **`forklifts."SN"`** | ✅ **เปลี่ยนเป็น `SN`** |
| `Sale.forklift_unit_no` | `sales.forklift_unit_no` | ❌ **ห้ามแตะ** — ชื่อตรงอยู่แล้ว |
| `InspectionRecord.unit_no` | `inspections.unit_no` | ❌ **ห้ามแตะ** — ชื่อตรงอยู่แล้ว |

> ห้ามใช้ find-replace ทั้งโปรเจกต์เด็ดขาด จะพังทั้ง `sales` และ `inspections`

### รายการงาน

- ☑ **A.1–A.6 (27 ก.ค.)** แก้ครบ 60 จุดใน 7 ไฟล์ ด้วย [scripts/rename-unitno-to-sn.mjs](scripts/rename-unitno-to-sn.mjs) — `types.ts` 1 · `mockData.ts` 15 · `forkliftCsv.ts` 4 · `stock/main` 19 · `sales/main` 13 · `transporter/main` 8 · cache key → `salesos_forklifts_v3`
- ☑ **A.7 ทดสอบ** — `npx tsc --noEmit` ผ่าน · `npm run build` ผ่าน (13 หน้า static) · lint เหลือ error 10 จุดซึ่ง**เป็นของเดิมทั้งหมด** (React hooks rules ในหน้า login/AiAssistant ที่ไม่ได้แตะ)
- ☑ **commit** `bdc6d75` — พร้อม push
- 🔴 **A.8 deploy — ยังทำไม่ได้** บัญชี `woralakpor789-cyber` ไม่มีสิทธิ์เขียน repo `goodrich2522-alt/sales-os-app.github.io` (HTTP 403)
  - **ต้องทำ:** เชิญ `woralakpor789-cyber` เป็น Collaborator ที่ repo → Settings ▸ Collaborators · หรือ push จากบัญชี `goodrich2522-alt` เอง
  - จนกว่าจะ deploy ทีมยังบันทึกรถผ่านแอปไม่ได้ (โค้ดบนเว็บยังเป็นเวอร์ชันเก่า)
- ☐ **A.9 ทดสอบบนเว็บจริงหลัง deploy** — เพิ่มรถ 1 คันแล้วรีเฟรช ต้องยังอยู่ (ข้อพิสูจน์ว่าแก้ถูก)

---

## เฟส B 🟠 — ซิงก์ข้อมูลให้ตรงกับ Excel

> รายละเอียดเต็มอยู่ที่ [DATA-SYNC-PLAN.md](DATA-SYNC-PLAN.md) · เฟส 1-2 เสร็จแล้ว (golden dataset 641 คัน)
> **ต้องทำหลังเฟส A** เพราะถ้าซิงก์ข้อมูลเข้าไปตอนที่แอปยังอ่าน/เขียนไม่ได้ ก็ไม่มีประโยชน์

- ☑ B.1 (27 ก.ค.) เคาะกติกาแปลงข้อมูล → [sync-rules.md](sync-rules.md)
- ☑ B.2 (27 ก.ค.) สร้าง golden dataset 641 คัน → `local-data/golden-stock.json`
- ☑ B.3 (27 ก.ค.) รายงานส่วนต่าง → เพิ่ม 233 · แก้ 408 · ต้องตรวจ 4 · ผู้ใช้เคาะกลุ่มขัดแย้ง A–E ครบ
- ☑ B.4 (27 ก.ค.) เพิ่ม `สั่งผลิต` · `รถเช่า` · `เคลม/รับกลับ` เข้า `fieldConfig` แล้ว (พร้อมยี่ห้อ/กลุ่มรถ/พิกัดใหม่)
- ☑ B.5 (27 ก.ค.) **นำเข้าสำเร็จ 641/641 แถว** — backup ไว้ที่ `local-data/backup-2026-07-27_1343/`
- ☑ B.6 (27 ก.ค.) ตรวจรับผ่าน — รถ **645 คัน** · **ดีลกำพร้าเหลือ 0 จาก 51** · วันที่เป็น ISO 257 แถว · สุ่มเทียบ 8 SN ถูกต้องทุกคัน

**ผลลัพธ์:** ดีล 51 รายการที่เคยชี้ไปที่รถที่ไม่มีจริง กลับมาผูกครบทุกรายการอัตโนมัติแล้ว

---

## เฟส C 🔴 — ปิดช่องโหว่ข้อมูลรั่ว (RLS)

> รายละเอียดอยู่ที่ [DEV-PLAN.md](DEV-PLAN.md) เฟส 1 · โครงสร้าง SQL + policy + Google Auth **ทำเสร็จและทดสอบผ่าน 20/20 แล้ว**
> เหลือแค่ "สับสวิตช์" แต่ยังสับไม่ได้เพราะติด 1 เรื่อง (ข้อ C.1)

> ⚠️ **ตราบใดที่ยังไม่ทำ ข้อมูลลูกค้าและราคาทุนทั้งบริษัทเปิดให้ใครก็ดึงได้** — เว็บอยู่บน GitHub Pages สาธารณะ ใครเปิด DevTools ก็เห็น anon key

- ☐ **C.1 ตัดสินใจเรื่องหน้าผู้ขนส่ง** ⬅️ **ต้องเคาะก่อน** — ผู้ขนส่งล็อกอินด้วยชื่อ+เบอร์ (ไม่มี Supabase session) พอเปิด RLS จะเขียนข้อมูลไม่ได้เลย
  - **ทาง A:** เปิด anonymous sign-in + ให้ policy เฉพาะ anon แตะ `forklifts`/`inspections` ได้ (ไม่แตะ `sales`)
  - **ทาง B:** ทำ RPC `receive_forklift()` / `deliver_forklift()` แบบ SECURITY DEFINER — คุมแคบกว่าแต่เขียนโค้ดเยอะกว่า
- ☐ **C.2 ทดสอบล็อกอินจริงทุก role บนเว็บ production** (ทำก่อนสับสวิตช์)
- ☐ **C.3 สับสวิตช์** — ลบ policy `open_all` ออกจากทั้ง 4 ตาราง (`forklifts`, `sales`, `inspections`, `app_config`)
- ☐ **C.4 เปลี่ยน anon key ใหม่** — key เดิมถือว่าหลุดแล้ว
- ☐ **C.5 ใส่ token ที่ GAS endpoint** (ตอนนี้ใครก็ยิงรูปเข้าได้)

**เกณฑ์ผ่าน:** เปิดเบราว์เซอร์ใหม่ที่ยังไม่ล็อกอิน ยิง REST ด้วย anon key → ต้องได้ **0 แถว** ทุกตาราง

---

## เฟส D 🟠 — สิทธิ์ตามบทบาทของจริง

> [DEV-PLAN.md](DEV-PLAN.md) เฟส 2 · ตอนนี้กันได้แค่ระดับ UI — anon key ยังเขียนตรงได้

- ☐ D.1 สร้างตาราง `profiles` (uid → ชื่อ, role) ผูกกับ Supabase Auth · เลิกใช้ `knownUsers` ใน `app_config`
- ☐ D.2 RLS แยกตาม role: ผู้ขนส่งเขียนได้แค่ `inspections` · เซลล์เขียน `sales` · สต็อกเขียน `forklifts` · การลบจำกัดเฉพาะ role ที่กำหนด
- ☐ D.3 ลดการเก็บข้อมูลลูกค้าไว้ใน localStorage — ตอนนี้ค้างอยู่ในทุกเครื่องที่เคยเปิด รวมมือถือคนขนส่ง

---

## เฟส E 🟡 — แก้บั๊กค้าง

> [DEV-PLAN.md](DEV-PLAN.md) เฟส 4 · ทำแทรกระหว่างเฟสอื่นได้

- ☐ E.1 `lastLocalEditRef` ไม่ทำงานจริง → optimistic update ถูก realtime ทับได้
- ☐ E.2 `sale`/`inspection` ยังใช้ `Date.now()` เป็น id (รถแก้เป็นรหัสสินค้าแล้ว)
- ☐ E.3 สถานะ "รอรับ" ไม่มีใน `DEFAULT_STOCK_STATUSES` → ฟีเจอร์รับรถตาม PI หารถไม่เจอ
- ☐ E.4 กราฟรายเดือนไม่แยกปี → ยอดคนละปีบวกรวมเดือนเดียวกัน
- ☐ E.5 แกลเลอรีตรวจรถโชว์สเปกจาก mock (ข้อมูลปลอม) → เปลี่ยนไปหาจาก `forklifts` จริง
- ☐ E.6 ลบดีลแล้วรีเซ็ตสถานะรถผิด — hard-code "พร้อมขาย" แม้รถเดิมสถานะอื่น
- ☐ E.7 `fieldConfig` ทับกันข้ามเครื่อง (lost update) — สองคนแก้พร้อมกัน ตัวเลือกหาย

---

## เฟส F 🟡 — ลดโค้ดซ้ำ

> [DEV-PLAN.md](DEV-PLAN.md) เฟส 3 · ตัดได้ ~600-800 บรรทัด · ไม่เร่ง ทำแทรกได้

- ☐ F.1 ลบโค้ดตาย: `lib/supabase.ts`, mock ที่ไม่ใช้ (มี password "1234" ฝังอยู่ 2 ตัว — ควรลบเร็วหน่อย), ถอด `next-pwa` ที่ไม่ได้ใช้
- ☐ F.2 แยก component ร่วม: `<Lightbox>` (ซ้ำ 4 ชุด), `<OptionsEditor>` + `<CustomFieldWizard>` (ซ้ำ ~250 บรรทัด × 2)
- ☐ F.3 สร้าง `lib/constants.ts` + `lib/format.ts` รวมค่าคงที่และ helper ที่ซ้ำ

---

## เฟส G 🟢 — ฟีเจอร์ใหม่ (เรียงตามมูลค่าทางธุรกิจ)

> ธุรกิจมีรายได้ 3 ขา — **ขาย · เช่า · ซ่อม** แต่แอปตอนนี้รองรับแค่ขาย
> เฟสนี้เริ่มได้หลังเฟส A-C เสร็จ (ข้อมูลถูกต้อง + ปลอดภัยแล้วค่อยต่อยอด)

### G.1 🚚 ระบบรถเช่า — **แนะนำทำก่อน**
มีรถเช่าจริง 11 คัน สัญญายาวถึง 5 ปี · **และเฟส 6 พบเอกสารค่าเช่า 1,366 ใบ รวม 22,099,359 บาท** เก็บไว้แล้วที่ `local-data/invoice-docs.json` (ประเภทงาน = "งานเช่า") — ต่อระบบได้ทันทีโดยไม่ต้องกรอกใหม่
- ตารางสัญญาเช่า: เลขสัญญา (GR-xxx), ลูกค้า, วันเริ่ม-สิ้นสุด, ค่าเช่ารายเดือน, รถที่ผูก
- แจ้งเตือนสัญญาใกล้หมด · รายงานรายได้ค่าเช่าต่อเดือน · รถเช่าคันไหนอยู่ที่ลูกค้ารายใด
- **ทำไมคุ้ม:** เป็นรายได้ประจำ (recurring) ที่ตอนนี้ตามด้วยมือทั้งหมด · ลูกค้า PO รายปีต้องสั่งรถใหม่มาทำรถเช่า — ต้องวางแผนล่วงหน้าได้

### G.2 🔧 ระบบงานซ่อม / บริการหลังการขาย
`SaleType` มีค่า "งานซ่อม" อยู่แล้วแต่ไม่มีหน้าจอรองรับ · **เฟส 6 พบเอกสารงานซ่อม 923 ใบ รวม 9,094,058 บาท** พร้อมใช้
- รับแจ้งซ่อม → ประเมินราคา → มอบหมายช่าง → ปิดงาน (ตรงกับ SOP ที่บริษัทใช้อยู่)
- ประวัติซ่อมรายคัน (ผูกกับ SN) · ค่าใช้จ่ายอะไหล่ · รายได้ค่าบริการ

### G.3 📦 ติดตามรถสั่งผลิต (PI Pipeline)
เฟส B ทำให้ระบบเห็นรถสั่งผลิต 16 คัน (ทุน 6.6 ล้าน) เป็นครั้งแรก
- หน้าไทม์ไลน์: สั่งผลิต → ผลิตเสร็จ → เข้าท่า → รับเข้าคลัง → ส่งมอบ
- **เติม SN ทีหลังได้** (ตอนสั่งมีแค่ลูกค้า+เซลล์ SN มาทีหลังตอนผลิตเสร็จ)
- แจ้งเตือนรถที่สั่งนานแล้วยังไม่เข้า

### G.4 💰 รายงานกำไรต่อคัน / ต่อเซลล์
มี `cost_price` และ `actual_sale` อยู่แล้ว แต่ยังไม่มีหน้ารายงานกำไร
- กำไรต่อคัน · ต่อรุ่น · ต่อเซลล์ · ต่อเดือน — ตรงกับเป้าหมายบริษัท "ลดต้นทุน เพิ่มกำไรสุทธิ"

### G.5 🏢 มุมมองรายสาขา
บริษัทมี 3 สาขา (ขอนแก่น ชลบุรี สมุทรปราการ) · ฟิลด์ `location` มีแล้วแต่ยังไม่มีหน้าสรุปแยกสาขา

### G.6 🤖 ผู้ช่วย "พี่เก่ง"
[components/AiAssistant.tsx](components/AiAssistant.tsx) — UI ครบแล้ว เหลือต่อสมองที่ฟังก์ชัน `getAssistantReply()` ไม่ต้องแตะ UI

### G.7 🔔 แจ้งเตือนรับประกัน / เปลี่ยนอะไหล่
ฟิลด์ `warranty_expiry` และ `parts_schedule` มีอยู่แล้วแต่ยังไม่มีระบบแจ้งเตือน — ต่อยอดง่าย ช่วยงานบริการหลังการขาย

---

## ลำดับที่แนะนำ

```
A (SN) → B (ซิงก์ข้อมูล) → C (ปิดช่องโหว่) → D (สิทธิ์) → G.1 (รถเช่า) → G.2 (ซ่อม) → ...
                                    ↑
                     E (บั๊ก) + F (โค้ดซ้ำ) แทรกได้ตลอดทาง
```

**เหตุผลของลำดับ:**
1. **A ก่อน** — ทีมสต็อกบันทึกข้อมูลไม่ได้อยู่ตอนนี้ และเป็นเงื่อนไขของ B
2. **B ต่อ** — ให้ข้อมูลในระบบตรงความจริงก่อน แล้วค่อยล็อกสิทธิ์ ไม่งั้นแก้ข้อมูลลำบาก
3. **C ให้เร็วที่สุด** — ทุกวันที่ปล่อยไว้คือข้อมูลลูกค้าเปิดโล่ง · เริ่มเคาะข้อ C.1 คู่ขนานไปกับ A/B ได้เลย
4. **G หลังสุด** — ต่อยอดบนข้อมูลที่ถูกต้องและปลอดภัยแล้วเท่านั้น
