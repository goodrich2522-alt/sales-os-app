-- ============================================================
-- sales: เพิ่มรูปหลักฐานการชำระเงิน (บังคับแนบตอนปิดการขาย) · 25 ก.ค. 2026
--   payment_proof = URL รูปสลิป/หลักฐาน (อัปโหลดผ่าน GAS→Drive เหมือนรูปตรวจรับ)
-- รันซ้ำได้ (idempotent) · รันบน production แล้ว
-- ============================================================
alter table sales add column if not exists payment_proof text;
